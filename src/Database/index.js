// Quando Refatorar o Codigo, vou utilizar esse arquivo para deixar apenas as func que acessam o banco

/*
import AsyncStorage from '@react-native-community/async-storage';
import uuidv1 from 'react-native-uuid';

async function salvarOrcamento(listOrcamento) {
    listOrcamento.id = uuidv1();
    nome,
    servico,
    valor,
};




*/
